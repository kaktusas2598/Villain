name: libVILLAIN CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: install dependencies
      run: sudo apt-get install -y libsdl2-dev
      run: sudo apt-get install -y libsdl2-mixer-dev
      run: sudo apt-get install -y libsdl2-image-dev
      run: sudo apt-get install -y libsdl2-ttf-dev
      run: sudo apt-get install -y liblua5.4-dev
      run: sudo apt-get install -y libassimp-dev
    - name: configure
      run: ./configure.sh
    - name: build
      run: ./build.sh
